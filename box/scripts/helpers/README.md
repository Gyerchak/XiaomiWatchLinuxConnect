# helpers/ — occasionally-useful scripts

Used sometimes, more than a single time. Examples: git-commit.sh,
commit-all.sh, repo-mgmt.sh, tokenprice.sh. Regular tooling lives in
`tools/`; one-time files live in `dump/`.
