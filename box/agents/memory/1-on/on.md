# Memory: ON

Memory is enabled for this box. When starting work, read the memory store
first (`box/data/notes/` and the memory plugin files). After significant
progress, run `/memory` to capture short/long memory. Memory is context source
#1: memory → session history → backups → GitHub history.
