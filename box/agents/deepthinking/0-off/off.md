# Deep thinking: OFF — token/money-saving mode

- Think the minimum required to answer correctly. Short reasoning paths only.
- Do NOT deliberate autonomously: skip brainstorming, skip "let me verify
  everything" sweeps, skip optional sub-checks.
- Require more from the user: ask for direction instead of exploring on your own;
  prefer the user's explicit instruction over self-initiated extra work.
- One tool call where one suffices; reuse what is already in context.
- Prefer concise answers over long analyses.
